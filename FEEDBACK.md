



- Whep and Whip endpoints are acronyms, all caps
- analytics endpoints
    - analytics endpoints have separate resource for each type of query instead of query parameter filters
    - startdate and enddate don't have examples or definition of date format
- account and geo are similar but resource organization of geo is off
- token has V1 in name, but there is no v2
- no api to get list of tracking ids
- list media assets -- both query parameters and body parameters for the same endpoint?
- https://docs.dolby.io/streaming-apis/reference/transcoder_gettranscoderinstance  says it takes an instance id, but its really the transcoder id
- why does a customer need the instance?
- why transcoder record id? what's a record?
- pagination without knowing the total number of records
